import React from "react";
import { render } from "@testing-library/react";

import Styleguide from "./Styleguide";
import { StyleguideProps } from "./Styleguide.types";

describe("Test Component", () => {
  let props: StyleguideProps;

  beforeEach(() => {
    props = {
      foo: "bar"
    };
  });


});
