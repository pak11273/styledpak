import React from "react";

import { AutoCompleteProps } from "./AutoComplete.types";

const AutoComplete: React.FC<AutoCompleteProps> = ({ foo }) => (
    <div>{foo}</div>
);

export default AutoComplete;

