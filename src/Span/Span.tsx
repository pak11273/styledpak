import {
  border,
  borderRadius,
  color,
  fontFamily,
  fontSize,
  fontStyle,
  fontWeight,
  layout,
  letterSpacing,
  lineHeight,
  padding,
  size,
  space,
  textAlign,
  textStyle,
} from "styled-system"

import styled from "styled-components"

export default styled.span`
  ${border}
  ${space}
  ${fontSize}
  ${fontStyle}
  ${size}
  ${color}
  ${layout}
  ${textStyle}
  ${letterSpacing}
  ${fontFamily}
  ${fontWeight}
  ${borderRadius}
  ${lineHeight}
  ${padding}
  ${textAlign}
`
